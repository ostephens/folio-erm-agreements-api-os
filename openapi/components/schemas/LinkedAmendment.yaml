type: object
properties:
  amendmentId:
    type: string
    format: uuid
  id:
    type: string
    format: uuid
  owner:
    type: string
    format: uuid
  status:
    oneOf:
      - type: string
      - $ref: ./RefData.yaml
